当前系统是ubuntu，当前目录是rayforce源码，执行(set data (.csv.read "/home/kindred/mywork/projects/data/cust_simple.csv"))的时候默认会直接读取CSV中的数据并且显示，这个体验不好。
帮我改成执行后只是保留数据文件路径，打开文件等准备工作，实际查询的时候，
比如执行：(.sql.eval "SELECT * from data where id<10")才真正去读取文件。
修改代码时注意跨平台，我要支持linux, windows(mingw64)