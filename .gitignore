*.o
*.d
*.a
*.so
*.dylib
rayforce
rayforce.test
build/
build_*/
.DS_Store
*.swp
gmon.out
.claude/settings.local.json

# Local tooling state — never commit
.wolf/
.claude/
rf_test_*.csv
.coderlm/
.worktrees/

CLAUDE.md
docs/plans/
docs/superpowers/
docs/design/

# IDE state
.idea/
.vscode/

# gcov / lcov artifacts
*.gcda
*.gcno
*.gcov
coverage*.info
coverage_html/
rayforce.cov
.playwright-mcp/
site/
.venv-docs/

# Benchmark binaries built into the repo root (the bench/ source dir is
# unaffected — it has no hyphen).
/bench-*

# Rayforce REPL history
.rayhist.dat

# Editor/analyzer compilation database (machine-specific paths)
compile_commands.json

# Extra build flavours (see Makefile)
rayforce.tsan
rayforce.test.tsan

# Fuzzing: grown corpus and crash artifacts stay local; only fuzz/seeds/ is committed
fuzz/corpus/
crash-*
leak-*
timeout-*
oom-*
